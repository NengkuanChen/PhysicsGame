using UnityEngine;

namespace Game.Editor.Build
{
    [CreateAssetMenu(fileName = "PackageInfoConfiguration(Android)",
        menuName = "Build/PackageInfoConfiguration(Android)",
        order = 0)]
    public class AndroidPackageInfoConfiguration : PackageInfoConfiguration
    {
    }
}